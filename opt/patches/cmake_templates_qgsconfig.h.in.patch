--- a/cmake_templates/qgsconfig.h.in	2025-05-15 15:26:35.218632070 +0300
+++ b/cmake_templates/qgsconfig.h.in	2025-05-15 14:46:59.465248999 +0300
@@ -31,6 +31,11 @@
 #define QGIS_LIB_SUBDIR "${QGIS_LIB_SUBDIR}"
 #define QGIS_QML_SUBDIR "${QGIS_QML_SUBDIR}"
 
+#define VENDOR_VERSION "${VERSION}"
+#define VENDOR "${PACKAGE_VENDOR}"
+#define VENDOR_DOMAIN "${PACKAGE_VENDOR_DOMAIN}"
+#define APP_NAME "${QGIS_APP_NAME}"
+
 #define QGIS_SERVER_MODULE_SUBDIR "${QGIS_SERVER_MODULE_SUBDIR}"
 
 #if !defined(QSCINTILLA_VERSION_STR)
